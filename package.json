{
  "name": "botsample",
  "version": "1.0.0",
  "description": "A telegram bot",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "telegraf": "^4.3.0",
    "typescript": "^4.2.3"
  }
}
